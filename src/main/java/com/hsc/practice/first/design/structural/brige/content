桥接模式：
    定义：将抽象与它的具体实现分类，使其两者都可以独立扩展(使用组合方式而不是继承)

    适用场景：1.系统中有两个变化的维度且这个两个维度都需要独立扩展
              2.希望抽象和实现分离
              3.不希望继承 继承会有很多多余的类。

    优点：1.抽象和具体实现分离
          2.符合开闭原则
          3.符合合成复用原则

    缺点：可读性差，需要识别出系统中两个独立变化的维度