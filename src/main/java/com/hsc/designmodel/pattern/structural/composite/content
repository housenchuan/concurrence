组合模式
    定义：将单个对象组合成树形结构，使客户端对单个对象和组合对象保持一致的方式处理。

    适用场景：1.客户端希望忽略组合对象和单个对象的差异。
              2.处理树形结构时。

    优点：让客户端忽略单个与组合的差异，便于操作；简化客户端的代码，符合开闭原则。

    缺点：限制类型时会比较复杂，使设计变得比较抽象。

    应用：ArrayList.addAll()  HashMap.addAll()

