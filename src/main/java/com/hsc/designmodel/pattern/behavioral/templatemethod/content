模板方法
    定义：定义了一个算法的骨架，并允许子类作为一个或多个步骤提供实现。
          模板方法使得子类在不改变算法结构的情况下，重新定义算法的某些步骤。

    适用场景：1.一次性实现一个算法的不变的部分，并将可变的行为留给子类来实现。
              2.各子类的公共的行为提取出来并集中到一个公共父类，从而避免代码重复。

     优点：提高复用性、提高扩展性、符合开闭原则

     缺点：类的数目增加、如果父类添加新的方法子类都得实现一遍。