访问者模式

    定义：封装作用于某数据结构(List/Set/Map)中的各元素的操作。
          可以在不改变各元素的类的前提下，定义作用于这些元素的操作。

    适用场景：一个数据结构(List/set/Map)包含很多类型的对象，数据结构和数据操作分离。

    优点：增加新的操作很容易，即新增一个访问者即可。

    缺点：增加新的数据结构困难，具体元素变更比较麻烦。

    理解：不同的访问者可以对相同的元素进行不同的操作
          同一个访问者可以对不同类型的元素进行不同的操作

    应用: BeanDefinitionVisitor FileVisitor